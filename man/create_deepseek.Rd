% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provider_deepseek.R
\name{create_deepseek}
\alias{create_deepseek}
\title{Create DeepSeek Provider}
\usage{
create_deepseek(
  api_key = NULL,
  base_url = NULL,
  headers = NULL,
  timeout_seconds = NULL,
  total_timeout_seconds = NULL,
  first_byte_timeout_seconds = NULL,
  connect_timeout_seconds = NULL,
  idle_timeout_seconds = NULL
)
}
\arguments{
\item{api_key}{DeepSeek API key. Defaults to DEEPSEEK_API_KEY env var.}

\item{base_url}{Base URL. Defaults to "https://api.deepseek.com".}

\item{headers}{Optional additional headers.}

\item{timeout_seconds}{Legacy alias for \code{total_timeout_seconds}.}

\item{total_timeout_seconds}{Optional total request timeout in seconds for API calls.}

\item{first_byte_timeout_seconds}{Optional time-to-first-byte timeout in seconds for API calls.}

\item{connect_timeout_seconds}{Optional connection-establishment timeout in seconds for API calls.}

\item{idle_timeout_seconds}{Optional stall timeout in seconds for API calls.}
}
\value{
A DeepSeekProvider object.
}
\description{
Factory function to create a DeepSeek provider.
}
\details{
DeepSeek supports classic aliases plus newer model families such as DeepSeek V4.

Common model IDs include:
\itemize{
\item \strong{deepseek-chat}: Chat alias provided by DeepSeek
\item \strong{deepseek-reasoner}: Reasoning alias provided by DeepSeek
\item \strong{deepseek-v4*}: DeepSeek V4 family model IDs exposed by the API
}

Additional DeepSeek-specific request fields such as \code{thinking},
\code{thinking_budget}, and \code{reasoning_effort} are passed through when supplied
to \verb{$generate()} or \verb{$stream()}.
}
\section{Supported Models}{

\itemize{
\item \strong{deepseek-v4-flash}: DeepSeek V4 Flash hybrid-thinking model (Reasoning, Tools, Structured) | ctx: 1000k
\item \strong{deepseek-v4-pro}: DeepSeek V4 Pro hybrid-thinking model (Reasoning, Tools, Structured) | ctx: 1000k
}
}

\examples{
\donttest{
if (interactive()) {
# Basic usage with deepseek-chat
deepseek <- create_deepseek()
model <- deepseek$language_model("deepseek-chat")
result <- generate_text(model, "Hello!")

# Using a reasoning-capable model
model_reasoner <- deepseek$language_model("deepseek-reasoner")
result <- model_reasoner$generate(
    messages = list(list(role = "user", content = "Solve: What is 15 * 23?")),
    max_tokens = 500,
    thinking = TRUE
)
print(result$text) # Final answer
print(result$reasoning) # Chain-of-thought reasoning

# Streaming with reasoning
stream_text(model_reasoner, "Explain quantum entanglement step by step")
}
}
}
